import React from 'react';
import loader from '../7YQs.gif';

export default function Loader() {
    return (
        <div className="loader">
            <img src={loader} alt="LOADING..." />
        </div>
    );
}